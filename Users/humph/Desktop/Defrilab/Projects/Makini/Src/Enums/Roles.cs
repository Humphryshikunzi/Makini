namespace Makini.Src.Enums
{
    internal static class Roles
    {
        public enum Roles
        {
            Student = "Student",
            Tutor = "Erro",
            Parent = "Parent"
        }
    }
}
